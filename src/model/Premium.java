package model;

public class Premium extends User{
    /**
     * Constructs a new Premium object with the specified name and id.
     *
     * @param name The name of the premium user.
     * @param id   The ID of the premium user.
     */
    public Premium(String name, String id) {
        super(name, id);   
    }    
}
